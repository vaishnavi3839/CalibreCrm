# Railway / Render monorepo note
#
# Do NOT deploy the repo root as one service.
# Create THREE things:
#   1) Postgres plugin
#   2) API service  → Root Directory = backend
#   3) Web service  → Root Directory = frontend
#
# See HOSTING_RAILWAY_RENDER.md
